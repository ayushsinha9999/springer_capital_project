# springer_capital_project
# Springer Capital - Referral Program Data Pipeline  ## Overview This repository contains a robust data pipeline built in Python (Pandas) to model a user referral program. It cleanses raw log data, performs complex timezone conversions, and implements strict business logic to evaluate valid and invalid referral rewards to prevent fraud.
